name: covid19tracker2
description: A new Flutter application for tracking COVID'19.

version: 1.0.0+11

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^1.1.2
  http: ^0.12.1
  fluttertoast: ^8.0.3
  vibration: ^1.7.3
  package_info: ^0.4.3+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/
